# Bases NSE default

Esta carpeta contiene la base NSE default precargada que usa la app cuando el usuario no sube una base NSE personalizada válida.

Archivo principal:
- `base_nse_default.csv`: catálogo editable con `key`, `ubica_geo`, `estado`, `municipio`, `est_socio` y `categoria_est_socio`.
